#include "main.h"

/**
 * mul - multiplies the two given inputs
 * @a - first integer
 * @b - second integer
 *
 * Return: multiplication result of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
